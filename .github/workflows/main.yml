name: My First GitHub Actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v3

     - name: run sample action
       run: echo "wel come to GitHub actions" 
      
   
